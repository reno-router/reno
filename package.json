{
  "name": "deno-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write 'src/**/*.ts' && git commit -am 'Fixes formatting'",
    "test": "deno run tests.ts",
    "start": "deno run --allow-net src/example/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "prettier": "1.17.1"
  }
}
