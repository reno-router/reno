{
  "name": "reno",
  "version": "0.0.1",
  "description": "A thin routing library designed to sit on top of Deno's standard HTTP module",
  "scripts": {
    "format": "prettier --write '**/*.ts' && git commit -am 'Fixes formatting'",
    "test": "deno run tests.ts",
    "start": "deno run --allow-net src/example/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "prettier": "1.17.1"
  }
}
